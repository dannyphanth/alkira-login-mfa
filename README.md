# Alkira UI Take-Home

Login + MFA exercise.

## Run

```bash
npm install
npm run dev
```

```bash
npm run test:run
```

Vite 6 is pinned because Vite 8 failed to start on Node 20.13 (rolldown native binding).
